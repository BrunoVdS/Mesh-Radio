#=== Install TAK server ==========================================
info "installing TAK server."


#Proberen te laten maken dooe chatGPT op basis van de PDF die we hebben en internet.

# Postgresql and PostGIS  installeren
# Java, versie checken en war nodig installeren of aanpassen dat 17.x gebruikt wordt.

# Nakijken of het installatiebestand op de locatie staat waar we het nodig hebben.
# Als het bestand er is, Verifying GPG signatures on DEB packages



#Insallatie zelf
# eerst proberen om te installeren via 4.2.2.8 yum Install From tak.gov
# De parameters die nodig zijn om het bestand Takserver.repo kunnen ingesteld door een promt in te wereken die er naar vraagt tijdens de installatie.
# Als hier de juiste parameters niet voor aanwezig zijn normale installatie waarbij we moeten vermelden dat het bestand naar de PI gecopieerd moet worden

# Als dat niet lukt overgaan tot de installatie van op een bestand op de PI.

#Einde van het script op het punt dat de truststore en keystore moeten worden ingesteld.




# Optie voorzien om een dual server installatie te doen of een single??
# Dit dan op basis of het OS, Ubuntu is of Raspberry OS. We hebben toch al een variabele die deze gegevens heeft.
		# Opzoeken of een TAK server op Raspberi PI 5, 16GB kan draaien met 2 servers.
		# Vraag stellen aan chatGPT om te kijken of het mogelijk zou zijn. (systeem vereisten)
		# Wat zijn de voordelen van een dual server opstelling.

------


#na de installatiefine een configuratie file maken om de basis installatei te kunnen maken. Op basis van de pdf die we hebben.

# Use TAK with all services.
# Altijd starten bij opstart/reboot

#Alle variabelen die nodig zijn deifignieren en via een prompt laten ingeven tijdens de installatie.

# Alle certs en keystore bestanden die nodig zijn om bijvoorbeeld een connectie te kunnen maken met de server vanuit Google chrome automatisch downloaden naar
# USB stick die in steekt.

# Logging van de configuratie.


------


# Firewall configuratie
# Doen we dt best in een apart stuk omdat er nog services zijn die firewall rules nodig hebben zoals MediaMTX?

# SVraag stellen aan ChatGpt voor best practices.

------


# Script voorzien om een update te doen van de TAK server.
# Apart maar wel eenvoudig in gebruik te maekn bijvoorbeeld via een knop/link op het bureaublad????

# Moet update vorzien zowel voor de single als dual server opties en met de update via download via server en als het bestand op de hardware is gecopieerd.



